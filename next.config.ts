import type { NextConfig } from "next";

const nextConfig: NextConfig = {

reactStrictMode:false,
  images: {
 domains: ['graceintlgroup.com','mloimffbzuc0.i.optimole.com','keia.local','images.pexels.com'],
  },
};

export default nextConfig;
